locales:
  - en-US
  - zh-CN
atsa_database:
  default_path: ./data/players.csv
match:
  batches:
    - name: First Call
      interval: 0
      voice: no
    - name: Second Call
      interval: 30
      voice: no
    - name: Final call
      interval: 120
      voice: yes
